# 🌐 [Название проекта] - Веб-приложение Google Apps Script

> **[Название проекта]** — это [Краткое описание: что это и для чего]. Это веб-приложение предоставляет [описание решения/преимуществ] через удобный пользовательский интерфейс.

![License](https://img.shields.io/badge/license-MIT-blue.svg) ![GAS](https://img.shields.io/badge/platform-Google%20Apps%20Script-green.svg) ![Version](https://img.shields.io/badge/version-1.0.0-orange.svg)

<a name="toc"></a>
## 📖 Содержание

1. [🚀 Установка и настройка](#install)
2. [🚀 Быстрый старт](#quickstart)
3. [API бэкенда: Серверные функции](#backend_api)
4. [Фронтенд: Взаимодействие на стороне клиента](#frontend_interaction)
5. [Структура данных](#data_methods)
6. [💡 Примеры использования](#examples)
7. [⚠️ Важные замечания](#notes)
8. [🎯 Рекомендации](#tips)
9. [🐛 Известные ограничения](#limits)

---

<a name="install"></a>
## 🚀 Установка и настройка
[меню](#toc) | [далее](#quickstart) | [назад](#toc)

### Установка зависимостей

```bash
npm install
```

### Настройка Clasp (для локальной разработки)

Установите Clasp глобально и войдите в систему:
```bash
npm install -g @google/clasp
clasp login
```



### Скрипты

| Команда | Описание |
| :--- | :--- |
| `npm run dev` | Запустить сервер для разработки с живой перезагрузкой для редактирования файлов фронтенда (HTML, Svelte). |
| `npm run build` | Собрать проект (фронтенд и бэкенд) в папку `dist/`. |
| `npm run push` | Сборка + отправка файлов `dist/Code.js` и `dist/*.html` в Google Apps Script. |
| `npm run deploy` | Полный цикл релиза: Сборка -> Отправка кода в GAS -> Создание новой версии развертывания веб-приложения. |

### Опции

Процесс сборки настраивается через `build.config.json`. Полное описание всех опций смотрите в [**Руководстве по конфигурации сборки (`DOCS/CONFIG.ru.md`)**](CONFIG.ru.md).

| Опция                   | Тип       | Описание                                                       |
| :---------------------- | :-------- | :------------------------------------------------------------- |
| `outDir`                | `String`  | Путь к выходному каталогу для собранных файлов.                |
| `manifest`              | `String`  | Путь к файлу манифеста `appsscript.json`.                      |
| `package`               | `String`  | Путь к `package.json`, используется для баннеров версий.       |
| `message`               | `String`  | Необязательно. Шаблон сообщения при успешном `push` или `deploy`. |
| `clasp`                 | `Object`  | Необязательно. Если есть, в `outDir` генерируется `.clasp.json`. |
| `frontend.build`        | `Boolean` | Включает сборку фронтенда.                                     |
| `frontend.src`          | `String`  | Исходный каталог для файлов фронтенда.                         |
| `frontend.minify`       | `Boolean` | Минифицирует итоговый HTML, JS и CSS.                          |
| `frontend.include`      | `Array`   | Дополнительные файлы/каталоги для включения в сборку фронтенда.|
| `backend.build`         | `Boolean` | Включает сборку бэкенда.                                       |
| `backend.src`           | `String`  | Исходный каталог для файлов бэкенда.                           |
| `backend.minify`        | `Boolean` | Минифицирует код бэкенда.                                      |
| `backend.concatenate`   | `Boolean` | Если `true`, объединяет все файлы бэкенда в один `outFile`.    |
| `backend.outFile`       | `String`  | Имя выходного файла, если `concatenate` равно `true`.          |
| `backend.include`       | `Array`   | Дополнительные файлы/каталоги для включения в сборку бэкенда.  |
| `backend.priorityOrder` | `Array`   | Определяет порядок файлов при конкатенации.                    |

---

<a name="quickstart"></a>
## 🚀 Быстрый старт
[меню](#toc) | [далее](#backend_api) | [назад](#install)

Чтобы запустить ваше веб-приложение:

1.  **Настройте `.clasp.json`**: Убедитесь, что `scriptId` соответствует ID вашего проекта Google Apps Script.
2.  **Разверните**: Выполните `npm run deploy`. Это соберет ваш проект, отправит код в Google Apps Script и создаст новую версию веб-приложения.
3.  **Доступ к веб-приложению**:
    *   После развертывания clasp выведет URL вашего веб-приложения.
    *   Либо перейдите в ваш проект Google Apps Script, нажмите «Развертывание» -> «Управление развертываниями» и найдите URL развернутого веб-приложения.

Затем вы можете открыть этот URL в браузере для взаимодействия с веб-приложением.

```javascript
// Пример базовой серверной функции для отображения веб-приложения
function doGet() {
  return HtmlService.createTemplateFromFile('index').evaluate()
      .setTitle('[Название проекта] Web App')
      .setFaviconUrl('https://www.google.com/favicon.ico'); // Необязательно
}

// Пример вызова серверной функции на стороне клиента
// В вашем фронтенд-коде JavaScript:
/*
google.script.run
  .withSuccessHandler(function(response) {
    console.log("Ответ сервера:", response);
  })
  .withFailureHandler(function(error) {
    console.error("Ошибка сервера:", error);
  })
  .myServerFunction('someClientData');
*/
```

---

<a name="backend_api"></a>
## API бэкенда: Серверные функции
[меню](#toc) | [далее](#frontend_interaction) | [назад](#quickstart)

Это функции Google Apps Script, выполняемые на сервере, которые обычно вызываются с фронтенда через `google.script.run`.

### `doGet(e)` / `doPost(e)`
Основные точки входа для запросов веб-приложения.
**Параметры:**
- `e` _(object)_ - Объект события, содержащий параметры запроса.

**Возвращает:** `HtmlOutput` или другое содержимое в зависимости от запроса.

```javascript
function doGet(e) {
  // Обработка GET-запросов, обычно для отображения основного HTML-файла
  return HtmlService.createTemplateFromFile('index').evaluate();
}

function doPost(e) {
  // Обработка POST-запросов, например, для отправки форм или вызовов API
  Logger.log('Получены данные POST-запроса: ' + JSON.stringify(e.postData.contents));
  return ContentService.createTextOutput(JSON.stringify({ status: 'success' }))
      .setMimeType(ContentService.MimeType.JSON);
}
```

### `myServerFunction(arg1, arg2)`
Краткое описание того, что делает эта серверная функция.
- `arg1` _(тип)_ - Описание аргумента.
- `arg2` _(тип)_ - Описание аргумента.
**Возвращает:** `Тип` (данные, возвращаемые клиенту).

```javascript
function myServerFunction(clientData) {
  Logger.log('Клиент вызвал myServerFunction с: ' + clientData);
  // Выполнение некоторой логики на стороне сервера
  return "Данные с сервера: " + clientData.toUpperCase();
}
```

---

<a name="frontend_interaction"></a>
## Фронтенд: Взаимодействие на стороне клиента
[меню](#toc) | [далее](#data_methods) | [назад](#backend_api)

В этом разделе описывается, как клиентская часть (HTML, JavaScript, Svelte) взаимодействует с бэкендом Google Apps Script и обрабатывает ввод пользователя.

### `callServer(functionName, ...args)` (Вспомогательная функция для `google.script.run`)
Описание метода.
**Параметры:**
- `functionName` _(string)_: Имя серверной функции для вызова.
- `...args` _(any)_: Аргументы, передаваемые в серверную функцию.

**Возвращает:** `Promise`, который разрешается с ответом сервера.

```javascript
// Пример использования вспомогательной обертки вокруг google.script.run
// (например, src/frontend/utils/callServer.js)
// В вашем клиентском JavaScript:
// import { callServer } from './utils/callServer.js';

/*
callServer('myServerFunction', 'Привет от клиента!')
  .then(response => {
    document.getElementById('output').innerText = response;
  })
  .catch(error => {
    console.error('Ошибка вызова серверной функции:', error);
  });
*/
```

---

<a name="data_methods"></a>
## Структура данных
[меню](#toc) | [далее](#examples) | [назад](#frontend_interaction)

Описание объектов данных, обычно передаваемых между фронтендом и бэкендом или используемых внутри.

### Объект `ClientToServerRequest`
```javascript
{
  action: "performAction", // Описание запрашиваемого действия
  payload: {
    key: "value", // Данные, связанные с действием
    id: 123
  }
}
```

### Объект `ServerToClientResponse`
```javascript
{
  status: "success", // или "error"
  message: "Операция успешно завершена.",
  data: {
    result: true // Любые данные, возвращаемые сервером
  }
}
```

---

<a name="examples"></a>
## 💡 Примеры использования
[меню](#toc) | [далее](#notes) | [назад](#data_methods)

### 1. Базовый вызов с фронтенда на бэкенд
Описание того, как фронтенд отправляет простой запрос на бэкенд и обрабатывает ответ.

```javascript
// Во фронтенд-коде JavaScript (например, в компоненте Svelte или скрипте index.html)
function fetchGreeting() {
  google.script.run
    .withSuccessHandler(function(greeting) {
      document.getElementById('greeting-display').innerText = greeting;
    })
    .withFailureHandler(function(error) {
      console.error('Ошибка при получении приветствия:', error.message);
    })
    .getGreeting('Мир'); // Вызывает серверную функцию с именем 'getGreeting'
}

// Соответствующая серверная функция (Code.js)
function getGreeting(name) {
  return 'Привет, ' + name + '! С сервера.';
}
```

### 2. Отправка формы с данными
Описание того, как обрабатывать отправку форм, передавая данные на бэкенд.

```html
<!-- В index.html или шаблоне компонента Svelte -->
<form id="dataForm">
  <input type="text" id="nameInput" placeholder="Ваше имя">
  <button type="submit">Отправить</button>
</form>

<script>
  document.getElementById('dataForm').addEventListener('submit', function(e) {
    e.preventDefault();
    const name = document.getElementById('nameInput').value;
    google.script.run
      .withSuccessHandler(function(response) {
        alert(response);
      })
      .withFailureHandler(function(error) {
        console.error('Ошибка отправки формы:', error.message);
      })
      .processFormData(name); // Вызывает серверную функцию 'processFormData'
  });
</script>

```
```javascript
// Соответствующая серверная функция (Code.js)
function processFormData(name) {
  Logger.log('Получены данные формы - Имя: ' + name);
  // Обработка данных, например, запись в таблицу
  return 'Спасибо, ' + name + '! Ваши данные были обработаны.';
}
```

### 3. Интеграция с сервисами Google (например, Google Таблицы)
Пример взаимодействия веб-приложения с другими сервисами Google.

```javascript
// Серверная функция (Code.js) для чтения данных из Google Таблицы
function getDataFromSheet() {
  const sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
  const range = sheet.getDataRange();
  const values = range.getValues();
  return values;
}

// Клиентский JavaScript для отображения данных
/*
google.script.run
  .withSuccessHandler(function(data) {
    const outputDiv = document.getElementById('sheet-data');
    outputDiv.innerHTML = '<h2>Данные из Таблицы:</h2>';
    data.forEach(row => {
      outputDiv.innerHTML += `<p>${row.join(', ')}</p>`;
    });
  })
  .withFailureHandler(function(error) {
    console.error('Ошибка при получении данных из таблицы:', error.message);
  })
  .getDataFromSheet();
*/
```

---

<a name="notes"></a>
## ⚠️ Важные замечания
[меню](#toc) | [далее](#tips) | [назад](#examples)

1.  **Замечание 1**: Важный нюанс (например, требования API Google, такие как включение определенных API в проекте Google Cloud).
2.  **Замечание 2**: Поведение при обработке ошибок (и клиентская, и серверная части должны быть надежными).
3.  **Авторизация**: Требуемые области OAuth для вашего веб-приложения. Убедитесь, что в `appsscript.json` перечислены все необходимые области для сервисов, к которым обращается ваше приложение (например, `https://www.googleapis.com/auth/spreadsheets`). Они автоматически определяются Google, но рекомендуется их проверять.

---

<a name="tips"></a>
## 🎯 Рекомендации
[меню](#toc) | [далее](#limits) | [назад](#notes)

### Производительность
Советы по оптимизации вашего веб-приложения, включая минимизацию вызовов клиент-сервер, эффективную обработку данных на бэкенде и оптимизацию ресурсов фронтенда.

### Безопасность
Рекомендации по обеспечению безопасности вашего веб-приложения, например, проверка вводимых данных как на фронтенде, так и на бэкенде, предотвращение утечки конфиденциальных данных и правильная аутентификация.

### Расширяемость
Как структурировать ваш проект для облегчения поддержки и добавления новых функций в будущем (например, модульные функции бэкенда, переиспользуемые компоненты фронтенда).

---

<a name="limits"></a>
## 🐛 Известные ограничения
[меню](#toc) | [назад](#tips)

1.  **Квоты Google**: Это веб-приложение подвержено стандартным квотам GAS (например, время выполнения скрипта, вызовы URL Fetch, общее количество триггеров).
2.  **Специфическое ограничение**: например, максимальный размер полезной нагрузки для `google.script.run`, проблемы совместимости с браузерами или ограничения определенных сервисов Google.
